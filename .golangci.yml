version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - asciicheck
    - bodyclose
    - err113
    - errcheck
    - errchkjson
    - errname
    - errorlint
    - exhaustive
    - exptostd
    - gochecknoinits
    - gocritic
    - godoclint
    - godot
    - gosec
    - govet
    - ineffassign
    - nilerr
    - revive
    - staticcheck
    - staticcheck
    - tagalign
    - tparallel
    - unconvert
    - unconvert
    - unparam
    - unused
    - usetesting
    - whitespace
    - wrapcheck
  settings:
    wrapcheck:
      ignore-sigs:
        - github.com/anexia/go-anxsdk/v1/common.MapTransportError
        - github.com/anexia/go-anxsdk/v2/common.MapTransportError
        - fmt.Errorf
    revive:
      enable-all-rules: true
      rules:
      - name: add-constant
        exclude: ["TEST"]
        arguments:
          - max-lit-count: "3"
            allow-strs: '""'
            allow-ints: "0,1,2"
            allow-floats: "0.0,0.,1.0,1.,2.0,2."
      - name: package-naming
        disabled: true
      - name: line-length-limit
        arguments: [ 180 ]
      - name: redundant-import-alias
        disabled: true
      - name: cognitive-complexity
        arguments: [ 12 ]
      - name: max-public-structs
        disabled: true

  exclusions:
    rules:
      - path: internal/example
        linters:
          - revive
          - errchkjson
      - path: _test\.go
        linters:
          - revive
          - errcheck
      - path: v1/.+\.go
        linters:
          - revive
        text: "max-public-structs"


formatters:
  enable:
    - goimports
    - gofmt